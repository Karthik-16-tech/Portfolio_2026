# NPM Configuration for consistent builds across environments
# Ensures reproducible installs

engine-strict=true
legacy-peer-deps=false

# Lock file version for consistency
lockfile-version=3

# Performance settings
fetch-timeout=60000
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
